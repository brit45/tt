#include <iostream>

#include "Chain.hpp"
#include <random>
#include <boost/archive/binary_oarchive.hpp>

int main(void) {

    CRYPTO_::Block block;

    srand(time(NULL));

    block.Init();

    return 0;
}